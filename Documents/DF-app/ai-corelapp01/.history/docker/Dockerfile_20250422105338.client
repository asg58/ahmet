FROM node:18-alpine

WORKDIR /app

# Copy package.json en package-lock.json
COPY package*.json ./

# Installeer dependencies
RUN npm install --legacy-peer-deps

# Installeer uuid expliciet
RUN npm install uuid @types/uuid --legacy-peer-deps

# Kopieer de rest van de applicatie
COPY . .

# Bouw de Next.js applicatie
RUN npm run build

# Expose port 3000
EXPOSE 3000

# Start de applicatie
CMD ["npm", "run", "dev"] 